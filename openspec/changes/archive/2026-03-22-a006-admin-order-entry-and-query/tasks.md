## 1. 資料模型與後端查詢

- [x] 1.1 為 `orders` 新增 nullable `created_by` 欄位與對 `employees.id` 的 foreign key migration
- [x] 1.2 擴充訂單 domain model、DTO 與 repository 查詢，支援 JOIN 員工、菜單、供應商與建立者資訊
- [x] 1.3 實作管理員查詢 API `GET /api/admin/orders`，支援 `date`、`employee_id` 篩選與管理員權限驗證

## 2. 管理員代訂流程

- [x] 2.1 在後端實作管理員代訂 service，重用既有訂單建立驗證並限制只能建立隔日訂單
- [x] 2.2 在代訂流程加入 A003 截止時間檢查，超過訂餐日前一日 17:00 時拒絕建立
- [x] 2.3 實作管理員代訂 API `POST /api/admin/orders`，成功時寫入 `orders.created_by`

## 3. 前端管理介面

- [x] 3.1 在管理員訂單總覽頁新增日期與員工篩選，顯示員工姓名、便當名稱、供應商與建立者資訊
- [x] 3.2 在管理員頁面新增代替員工建立隔日訂單的表單與送出流程
- [x] 3.3 補上截止時間提示、逾時錯誤訊息與成功後列表刷新行為

## 4. 驗證

- [x] 4.1 驗證管理員查詢 API 的日期/員工篩選、欄位內容與非管理員拒絕情境
- [x] 4.2 驗證管理員在截止前成功代訂、逾時失敗、非隔日失敗與 `created_by` 寫入正確
- [x] 4.3 驗證前端管理頁的查詢、代訂、錯誤提示與成功回寫流程
