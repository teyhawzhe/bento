## 1. Backend Contract

- [x] 1.1 在既有 A002 路徑中新增員工 `GET /api/calendar` 查詢，回傳指定年月的 `date` / `is_workday` 行事曆資料
- [x] 1.2 擴充員工端菜單 DTO 與查詢流程，讓 `GET /api/orders/menu` 回傳 `supplier_name` 並維持不顯示價格
- [x] 1.3 補上後端驗證與測試，確認員工行事曆查詢權限、菜單回傳欄位與既有截止規則未回歸

## 2. Employee Calendar UI

- [x] 2.1 將員工「訂便當」TAB 從日期下拉改為月行事曆視圖，整合當月工作日、訂單與可訂菜單狀態
- [x] 2.2 實作日期格互動視窗：空白可訂日顯示便當選單，已訂日期顯示已訂資訊與修改/取消入口
- [x] 2.3 移除下拉式主流程與相關文案，保留截止提示、不可互動日期樣式與員工端不顯示價格規則

## 3. Interaction And Verification

- [x] 3.1 將行事曆視窗中的修改與取消動作接回既有 MessageBox 確認流程
- [x] 3.2 在訂餐、改單、取消成功後統一刷新行事曆、菜單與個人訂單資料，確保日期格狀態同步更新
- [x] 3.3 補上前端/整合驗證，確認非上班日不可點擊、可訂日期可成功下單、已訂日期可修改或取消、逾期取消按鈕隱藏
