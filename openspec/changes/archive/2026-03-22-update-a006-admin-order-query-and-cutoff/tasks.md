## 1. 後端查詢與截止規則更新

- [x] 1.1 更新管理員訂單查詢 API 與 service，將篩選條件從單一 `date` 改為 `date_from`、`date_to`、`employee_id`
- [x] 1.2 在管理員訂單查詢流程加入今天～今天的預設日期區間，確保未提供日期時仍有一致結果
- [x] 1.3 將管理員代訂隔日便當的截止時間檢查從 `17:00` 改為當日 `16:30`

## 2. 前端管理員訂單頁同步

- [x] 2.1 更新前端 `GET /api/admin/orders` 呼叫與查詢 state，改用 `date_from`、`date_to`、`employee_id`
- [x] 2.2 將管理員訂單頁的預設查詢條件改為今天～今天，並在進入頁面時自動查詢
- [x] 2.3 更新管理員代訂提示與錯誤訊息，讓畫面明確反映 `16:30` 截止規則

## 3. 驗證

- [x] 3.1 驗證管理員未帶日期條件時，系統會以今天～今天查詢並回傳正確結果
- [x] 3.2 驗證管理員可用 `date_from`、`date_to`、`employee_id` 正確篩選訂單資料
- [x] 3.3 驗證管理員在當日 `16:30` 前可成功代訂隔日便當，`16:30` 後會被拒絕
